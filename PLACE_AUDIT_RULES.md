# Place Accuracy Audit 규칙

## 목적
장소 데이터(좌표, 주소, 전화번호, 영업 여부)의 정확성 + 중복 검출.

## check_type별 판정 기준

### data_accuracy (데이터 정확성)

**accurate:**
- Kakao 검색 결과와 이름/주소 일치
- 좌표가 실제 위치와 50m 이내
- 전화번호 일치 (또는 둘 다 없음)
- 카테고리가 실제 업종과 일치

**inaccurate — 다음 중 하나라도 해당:**
- 좌표가 실제 위치에서 50m 이상 벗어남
- 주소가 Kakao와 다름 (동/번지 불일치)
- 전화번호 불일치 (한쪽만 있는 경우 제외)
- 카테고리 오류 (예: 식당인데 kids_cafe로 분류)
- 이름이 현재 상호와 다름 (리브랜딩/양도)

**데이터 오류 심각도:**

| 오류 | 심각도 | 조치 |
|------|--------|------|
| 좌표 50~200m 오차 | 중 | 좌표 수정 |
| 좌표 200m+ 오차 | 상 | 이전/폐업 의심 → revalidate |
| 주소 동/구 불일치 | 상 | 이전 의심 |
| 전화번호 불일치 | 하 | 번호 업데이트 |
| 카테고리 오류 | 중 | 카테고리 수정 |
| 상호명 변경 | 중 | 이름 업데이트 |

### duplicate_suspect (중복 의심)

**duplicate 판정 기준 (모두 충족):**
- 동일 카테고리
- 100m 이내 거리
- 이름 토큰 유사도 50% 이상
- 실제로 같은 장소임을 확인 (체인 다른 지점이 아닌)

**duplicate 아닌 경우:**
- 같은 건물 내 다른 층/호 (예: 키즈카페 1호점/2호점)
- 체인 같은 이름이지만 실제 다른 지점 (주소 확인)
- 이전 후 신규 등록 (구 장소 비활성화 필요)

**중복 시 병합 규칙:**
- 더 많은 mention/리뷰가 있는 쪽을 유지
- kakao_place_id가 있는 쪽을 우선
- 수동 등록(manual) > 자동 등록(auto_promoted)

### closed_moved (폐업/이전)

**closed 판정 기준:**
- Kakao 검색에서 NOT FOUND
- 최근 블로그 리뷰 6개월 이상 없음
- 네이버 플레이스에서 "폐업" 표시

**moved 판정 기준:**
- Kakao 재검증 시 좌표 200m 이상 차이
- 같은 이름이지만 주소가 다른 구/동

## 검토 시 필요 데이터

| 데이터 | 용도 | 현재 상태 |
|--------|------|----------|
| place_name, category, address | 기본 식별 | 기록됨 |
| check_result (JSONB) | Kakao 비교 결과 | 기록됨 |
| **place_source** | 어떤 수집기에서 왔는지 | **미기록 → 보완 필요** |
| **place_created_at** | 등록 시점 (데이터 노후 판단) | **미기록 → 보완 필요** |
| **last_mentioned_at** | 최근 블로그 언급 (영업 확인) | places 테이블에 있음 |

## 피드백 절차

1. inaccurate → DB 직접 수정 (좌표/주소/전화번호)
2. closed → `is_active = false` 처리
3. duplicate → 병합 (mention 이전 + 비활성화)
4. 반복 패턴 발견 시 → place-gate.ts 필터 강화

## 사용자 피드백 검토 (place-feedback)

> 불변 원칙은 `AUDIT_RULES.md` "8종: 사용자 피드백 감사" 참조. 여기는 판정 기준·착안점(예시).

### 판정 기준

**recategorize (사용자가 분류 변경 — places.category 이미 즉시 반영됨)**
- 타당: 새 분류가 실제 업종과 일치 → 원인추적 후 동일원인 일괄수정, `approved`
- 부당: 사용자가 잘못 바꿈(장난/오인) → places.category 원복, `rejected` + audit_notes 기재
- 경계: 모호하면 `flagged` → 수동 재확인

**hide (사유와 함께 가리기)**
- `not_baby` → 비아기 패턴이면 place-gate / place_blacklist_patterns 보강 + 해당 place is_active 판정
- `closed` → 폐업·이전 확인 시 is_active=false (Kakao 재검증 권장)
- `other` → 집계만. 동일 place 다수 누적 시 패턴 분석
- 단일 사용자 단발은 신호 약함 — 동일 place 다수 누적을 우선 검토

### 원인추적 착안점 (source 별)

- **kakao**: 분류는 수집 시 `mapKakaoCategory` 가 결정. sub_category(=카카오 category_name 말단)가
  어느 패턴에도 안 잡히면 defaultCategory fallback. 재현 후 (a) 패턴 키워드 추가 (b) DB 일괄 정정
  (c) place_blacklist_patterns 중 택. 패턴 추가 시 4단계 4-2b 오탐 점검 필수.
- **그 외 source**: 해당 수집기의 category 할당 지점을 직접 확인 (현재 자동 추적 미지원 — 수동).

### 일괄수정 원칙

- 동일 잘못된 category + 동일 sub_category 정확 매칭이 기본 (느슨 매칭은 오탐 → 4-2b 적용)
- dry-run 으로 영향 범위 확인 후 적용. 유형당 상한 + 종료 보고서에 건수 기재

## 프로세스 보완 이력

### 2026-03-06 감사 결과
- **부정확률 68%** (15/22) — 홈플러스 계열 비아기친화 장소 13건 + 이벤트→장소 오등록 2건
- **대응**: 17건 비활성화 (13 inaccurate + 2 event-as-place + 2 closed)
- **근본 원인**: babygo 수집기가 이벤트를 장소로 등록 ("2026 롯데 루미나리에", "서울스프링페스티벌")
- **잔여 과제**:
  1. `place-gate.ts`에 홈플러스/부동산/인테리어/여행사 카테고리 필터 추가 필요 — **resolved** (2026-03-08: BLOCKED_CATEGORIES에 이미 포함)
  2. babygo 수집기에서 이벤트성 데이터가 places로 들어가는 경로 차단 필요 — **resolved** (2026-03-08: babygo.ts:278-281에서 event_starts_at+event_ends_at 있으면 createBabygoEvent 후 continue로 places INSERT 스킵)
  3. `place_source` + `place_created_at` 컬럼이 00057 마이그레이션으로 추가됨 — **resolved**

### 2026-03-06 전수 벌크 감사 (658건)
- **정확률 97.6%** (642/658) — 대부분 kakao 소스 장소가 정상
- **비활성화 16건**: 주점(3), 타이어(3), 자동차(2), 역사유적(5), 주유소(1), 묘지(1), 기타(1)
- **근본 원인**: kakao 검색에서 "아기" 키워드 매칭 결과가 비아기친화 장소 포함
  - "아기자동차" (중고차), "아기사슴" (식당명), 역사유적 ("한원부원군묘", "덕혜옹주묘")
  - place-gate BABY_NAME_WHITELIST에 "아기" 포함 → 비아기 장소도 통과
- **잔여 과제**: kakao 수집기에서 카테고리 검증 강화 필요 — **resolved** (2026-03-08: BLOCKED_CATEGORIES에 자동차정비/자동차수리/타이어/주점/주유소 이미 포함)
- **classifier-config.json v2**: 스카이런/마라톤 블랙리스트 + 상업 프로모션 패턴 추가

### 2026-03-10 감사 (133건 pending → 0건)
- **비활성화 85건**: 산(17), 하천/저수지(14), 고개/골(16), 역사유적/묘역(20), 폭포/바위/등산로(9), 기타 비아기(9)
- **승인 48건**: 어린이공원/놀이터(15), 체험시설(8), 키즈시설(5), 공원(10), 기타 정상(10)
- **근본 원인**: kakao 수집기가 동두천/화성남양/의왕 일대의 관광명소를 "놀이" 카테고리로 대량 수집
  - 소요산 관련 17건 (일주문, 폭포, 바위, 봉우리 등), 화성 남양읍 관련 30건 (하천, 묘역, 고개)
  - 의왕시 관련 20건 (모락산, 오봉산, 왕송호수 등)
- **코드 개선 (4단계)**:
  1. `place-gate.ts` BLOCKED_NAME_PATTERNS 확장: `묘역$|추모비$|순절비$|선정비$|장군묘|행궁지$|저수지$|등산로$|산림욕장$|관광특구$` 추가
  2. `place-accuracy-audit.ts` NOT_BABY_FRIENDLY_PATTERNS 확장: 산/봉/천/골/폭포/등산로/묘역/추모비 등 자연지형+역사유적 패턴 추가
  3. 교차 감사: inactive 장소에 연결된 38 mention → score=0 + locked 처리
- **패턴 예시** (다음 감사 참고):
  - 산: 천마산, 무봉산, 해룡산, 칠봉산, 모락산, 오봉산, 덕성산, 함봉산
  - 하천: 상패천, 신남천, 무봉천, 남양천, 자안천, 황구지천, 왕곡천
  - 역사: 어유소장군묘역, 홍덕문선생추모비, 윤계선생순절비, 이태조행궁지, 삼충단
  - 자연: 소요산넓적바위, 소요산원효폭포, 왕방계곡배꼽다리, 매바위, 온석골
