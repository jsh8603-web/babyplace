# Classification Audit 규칙

## 목적
이벤트 아기 관련성 분류(3단계: blacklist → whitelist → LLM)의 정확도 검증.

## 판정 기준

### correct (올바른 분류)

**포함(included)이 correct인 경우:**
- 대상 연령이 영유아/어린이/가족 명시 (USE_TRGT에 "유아", "어린이", "전연령" 등)
- 타이틀에 키즈/어린이/아기/가족 키워드 포함
- 실제 아기 동반 가능 행사 (키즈카페, 어린이 뮤지컬, 가족 체험, 캐릭터 전시 등)
- 무료 문화 행사로 연령 제한 없음

**제외(excluded)가 correct인 경우:**
- 성인 전용 (술, 클럽, 19금, 성인 공연)
- 미취학 입장불가 명시
- 학술/전문가 행사 (논문, 세미나, 학회)
- 개인전/작가전 (아기 무관)
- 경쟁/격투/극한 스포츠

### false_positive (잘못 포함됨 — 제외해야 함)
- "어린이" 키워드 포함이지만 실제 아기 부적합 (예: "어린이대공원 야간 마라톤" → 참가 불가)
- 캐릭터명이 트리거했지만 성인 대상 (예: "건담 프라모델 대회")
- 가족 키워드 있으나 고연령 대상 (예: "가족과 함께하는 등산 대회")
- LLM이 과대 해석 (예: "봄꽃 축제" → 아기 관련 아님에도 포함)

### false_negative (잘못 제외됨 — 포함해야 함)
- 블랙리스트 과차단 (예: "개인전" 패턴이 "어린이 그림 개인전" 차단)
- USE_TRGT 없어서 LLM 판단인데, LLM이 놓친 경우
- 제목만으로는 아기 관련 불명확하나 실제 내용은 키즈 행사

## 단계별 검토 포인트

| classifier_step | 검토 내용 | 흔한 오류 |
|-----------------|----------|----------|
| **blacklist** | 차단 패턴이 정당한가? | 복합어 과차단 ("개인전" → "어린이 개인전") |
| **whitelist** | 매칭 패턴이 적절한가? | 캐릭터명이 성인 콘텐츠에도 매칭 |
| **llm** | LLM 판단 근거가 합리적인가? | 모호한 행사에 과대/과소 해석 |
| **fallback** | regex 대체 판단이 맞는가? | LLM 실패 시 보수적 포함 |

## 검토 시 필요 데이터

| 데이터 | 용도 | 현재 상태 |
|--------|------|----------|
| event_name + use_target | 기본 판단 재료 | 기록됨 |
| classifier_step | 어느 단계에서 결정됐는지 | 기록됨 |
| **matched_pattern** | 어떤 regex가 트리거했는지 | **미기록 → 보완 필요** |
| **llm_confidence** | LLM 확신도 (0~1) | **미기록 → 보완 필요** |
| **is_fallback** | LLM 실패로 regex 대체 여부 | **미기록 → 보완 필요** |

## 이미지 안전성 (미구현 — 구조적 한계)

현재 파이프라인은 이미지 내용을 분석하지 않음. 분류기는 텍스트(제목/설명/USE_TRGT)만으로 판단.

**발견 사례: "1+1 이벤트" (#8649)**
- exhibition_extraction에서 터프팅공방 상업 프로모션을 문화행사로 수집
- 조치: 이벤트 삭제 + BLACKLIST_TITLE_PATTERN에 상업 프로모션 패턴 추가 (`1+1|할인 이벤트|구매.*이벤트|프로모션|특가 이벤트`)

**발견 사례: "9개의 시선" (#8882)**
- LLM 분류: `included` (문화행사로 판정)
- 실제: 공포/호러 일러스트 전시 → 포스터가 아기 앱에 극히 부적절
- 원인: 제목 "9개의 시선"만으로는 공포 전시 판별 불가

**향후 대응 옵션**:
- Gemini Vision으로 포스터 이미지 안전성 체크 추가 (비용/지연 증가)
- 포스터 없는 이벤트의 분류 시 source_url 크롤링하여 설명 확보

## 피드백 절차

1. `--patterns`로 false_positive/false_negative 패턴 분석
2. **false_positive 다수 → 해당 whitelist 패턴 조건 강화** (예: `캐릭터.*전시` → `캐릭터.*전시(?!.*프라모델)`)
3. **false_negative 다수 → blacklist 패턴 완화** 또는 whitelist 패턴 추가
4. `classifier-config.json` version 증가 + changelog 기록
5. 다음 배치에서 동일 이벤트 재분류 결과 비교

## 프로세스 보완 이력

### 2026-03-10 감사 (50건 → 41 correct, 6 FP, 3 FN)
- **정확률 82%** (41/50)
- **false_positive 6건** (잘못 포함):
  - 쉴드 샴푸 → 상업/광고 (블랙리스트 "샴푸" 추가)
  - 아이엔지홈 → 인테리어 업체 (블랙리스트 "인테리어" 추가)
  - 필립 그뤽 개인전 → 성인 미술 전시 (블랙리스트 "개인전" 추가)
  - 뮤지컬 빨래 → 성인 뮤지컬 (블랙리스트 "뮤지컬 빨래" 추가)
  - 노원/헤일리 전시 → 성인 전시
- **false_negative 3건** (잘못 제외):
  - 마곡 베이비페어 → "베이비" 키워드 있으나 미감지 (화이트리스트 "베이비페어" 추가)
  - 고고다이노 → 어린이 캐릭터 공연 (화이트리스트 "고고다이노", "다이노" 추가)
  - 가족과 함께하는 우주여행 → "가족" 키워드 있으나 LLM 제외 판단
- **코드 개선 (4단계)**:
  - `classifier-config.json` v4→v5: 블랙리스트 4건 + 화이트리스트 3건 추가
  - **주의**: "개인전" 블랙리스트는 "어린이 그림 개인전" 같은 FN 유발 가능 → 화이트리스트 "어린이.*개인전" 추가 검토 필요
