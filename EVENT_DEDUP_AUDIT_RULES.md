# Event Dedup Audit 규칙

## 목적
이벤트 중복 병합 결정의 정확성 검증 + 놓친 중복 발견.

## 판정 기준

### correct_merge (올바른 병합)
- 동일 행사의 다른 소스 등록 (예: seoul_events + blog_discovery 동일 축제)
- 이름이 거의 동일 (Dice similarity > 0.85)
- 장소(venue)와 날짜 범위가 겹침
- 하나가 다른 하나의 부분집합 (예: "서울숲 봄꽃축제" vs "서울숲 봄꽃축제 2026")

### false_merge (잘못된 병합 — 병합하면 안 됨)
- **같은 장소, 다른 행사**: "롯데월드 할로윈" vs "롯데월드 크리스마스"
- **같은 이름, 다른 지역**: "벚꽃축제(여의도)" vs "벚꽃축제(석촌호수)"
- **연례 행사 다른 연도**: "2025 서울빛축제" vs "2026 서울빛축제"
- **시리즈/회차 다른 행사**: "키즈콘서트 1회" vs "키즈콘서트 2회"

### missed_dupe (놓친 중복)
- 이름 표기 차이로 미감지 (예: "뽀로로 뮤지컬" vs "뽀로로와 친구들 뮤지컬")
- 영문/한글 혼용 (예: "PAW Patrol Show" vs "퍼피구조대 공연")
- 소스별 제목 차이 (예: 공식명 vs 블로그 약칭)

## match_reason별 검토 포인트

| match_reason | 의미 | 오류 위험 | 검토 포인트 |
|-------------|------|----------|------------|
| `exact_match` | source_id 동일 | 매우 낮음 | 거의 항상 correct |
| `venue_name` | 같은 장소 + 이름 유사 | 중간 | **날짜 범위 겹침 확인 필수** |
| `name_date` | 이름 유사 + 날짜 겹침 | 중간 | **장소 동일 확인 필수** |
| `high_name_sim` | Dice > 0.9 | 낮음 | 시리즈/회차 구분 확인 |

## similarity_score 해석

| 점수 | 해석 | 검토 강도 |
|------|------|----------|
| 0.95~1.0 | 거의 동일 | 빠른 확인 (날짜만 체크) |
| 0.85~0.95 | 높은 유사도 | 장소 + 날짜 확인 |
| 0.7~0.85 | 중간 유사도 | **상세 검토 필수** (장소, 날짜, 내용) |
| 0.6~0.7 | 경계선 | 반드시 수동 확인 (missed_dupe 스캔 대상) |

## 검토 시 필요 데이터

| 데이터 | 용도 | 현재 상태 |
|--------|------|----------|
| kept/removed event name | 기본 비교 | 기록됨 |
| similarity_score | 유사도 수치 | 기록됨 |
| match_reason | 감지 방법 | 기록됨 |
| **kept/removed source** | 어느 수집기에서 왔는지 | **미기록 → 보완 필요** |
| **kept/removed dates** | 날짜 범위 비교 | **미기록 → 보완 필요** |
| **venue_name** | 장소 동일 여부 | **미기록 → 보완 필요** |

## 피드백 절차

1. false_merge 발견 → event-dedup.ts similarity 임계값 상향 검토
2. missed_dupe 발견 → 정규화 로직 개선 (약칭, 영문/한글 변환)
3. venue_name 기반 오병합 → 날짜 범위 교차 검증 조건 추가
4. 임계값 변경 시 기존 데이터 재스캔으로 영향 분석
